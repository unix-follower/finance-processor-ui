apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fin-processor-ui.fullname" . }}-test-connection"
  labels:
    {{- include "fin-processor-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fin-processor-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
